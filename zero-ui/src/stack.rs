//! Stack layout widget, nodes and properties.
//!
//! # Full API
//!
//! See [`zero_ui_wgt_stack`] for the full widget API.

pub use zero_ui_wgt_stack::{
    get_index, get_index_fct, get_index_len, get_rev_index, is_even, is_first, is_last, is_odd, lazy_sample, lazy_size, node, stack_nodes,
    stack_nodes_layout_by, Stack, StackDirection, WidgetInfoStackExt,
};
