[package]
name = "integration-tests"
version = "0.0.0"
publish = false
edition = "2021"

[features]
default = ["ipc"]
ipc = ["zero-ui/ipc"]

[dev-dependencies]
zero-ui = { path = "../zero-ui", default-features = false, features = ["test_util"] }

[[test]]
name = "command"
path = "command.rs"

[[test]]
name = "focus"
path = "focus.rs"