[package]
name = "render-tests"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
zng-app = { path = "../../crates/zng-app" }
zng = { path = "../../crates/zng", features = [
    # "view",
    # "view_prebuilt",
    "multi_app",
] }
# use ZNG_VIEW_NO_INIT_START to manually select witch.
zng-view-prebuilt = { path = "../../crates/zng-view-prebuilt" }
zng-view = { path = "../../crates/zng-view" }

color-print = "0.3"
tracing = "0.1"