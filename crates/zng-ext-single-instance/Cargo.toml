[package]
name = "zng-ext-single-instance"
version = "0.9.1"
authors = ["The Zng Project Developers"]
edition = "2024"
license = "Apache-2.0 OR MIT"
readme = "README.md"
description = "Part of the zng project."
documentation = "https://zng-ui.github.io/doc/zng_ext_single_instance"
repository = "https://github.com/zng-ui/zng"
categories = ["gui"]
keywords = ["gui", "ui", "user-interface", "zng"]

[dependencies]
zng-env = { path = "../zng-env", version = "0.7.1", default-features = false }
zng-app = { path = "../zng-app", version = "0.19.1", default-features = false }
zng-txt = { path = "../zng-txt", version = "0.4.2", default-features = false }
zng-task = { path = "../zng-task", version = "0.9.0", default-features = false }
zng-ext-fs-watcher = { path = "../zng-ext-fs-watcher", version = "0.8.1", default-features = false }

parking_lot = { version = "0.12", default-features = false }
single-instance = { version = "0.3", default-features = false }
tracing = { version = "0.1", default-features = false }
serde_json = { version = "1.0", default-features = false, features = ["std"] }
dunce = {version = "1.0", default-features = false }
