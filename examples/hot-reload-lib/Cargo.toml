[package]
name = "zng-example-hot-reload-lib"
version = "0.0.0"
edition = "2024"
publish = false
description = "Hot reload library example for `hot_reload` example"

[lib]
crate-type = ["lib", "cdylib"]

[dependencies]
zng = { path = "../../crates/zng", features = ["hot_reload"] }
tracing = "0.1"