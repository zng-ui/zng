[package]
name = "zng-example-respawn"
version = "0.0.0"
publish = false
edition = "2024"

[dependencies]
zng = { path = "../../crates/zng", features = ["view"] }
zng-view = { path = "../../crates/zng-view" }
zng-app = { path = "../../crates/zng-app" }
tracing = "0.1"