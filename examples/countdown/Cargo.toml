[package]
name = "zng-example-countdown"
version = "0.0.0"
publish = false
edition = "2024"

[dependencies]
zng = { path = "../../crates/zng", features = ["view"] }
tracing = "0.1"
tracing-chrome = "0.7"
tracing-subscriber = "0.3"