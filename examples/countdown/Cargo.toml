[package]
name = "zng-example-countdown"
version = "0.0.0"
publish = false
edition = "2024"

[dependencies]
zng = { path = "../../crates/zng", features = ["view", "trace_recorder", "trace_widget", "trace_wgt_item"] }
tracing = "0.1"